///
/// HybridSeekbarComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridSeekbarComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSeekbarSpecSwift.hpp"
#import "Seekbar-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::seekbar;
using namespace margelo::nitro::seekbar::views;

/**
 * Represents the React Native View holder for the Nitro "Seekbar" HybridView.
 */
@interface HybridSeekbarComponent: RCTViewComponentView
@end

@implementation HybridSeekbarComponent {
  std::shared_ptr<HybridSeekbarSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSeekbarComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSeekbarComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridSeekbarSpec> hybridView = Seekbar::SeekbarAutolinking::createSeekbar();
    _hybridView = std::dynamic_pointer_cast<HybridSeekbarSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  Seekbar::HybridSeekbarSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const react::Props::Shared&)props
            oldProps:(const react::Props::Shared&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridSeekbarProps const>(props);
  auto& newViewProps = const_cast<HybridSeekbarProps&>(newViewPropsConst);
  Seekbar::HybridSeekbarSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // position: optional
  if (newViewProps.position.isDirty) {
    swiftPart.setPosition(newViewProps.position.value);
    newViewProps.position.isDirty = false;
  }
  // duration: optional
  if (newViewProps.duration.isDirty) {
    swiftPart.setDuration(newViewProps.duration.value);
    newViewProps.duration.isDirty = false;
  }
  // readAheadValue: optional
  if (newViewProps.readAheadValue.isDirty) {
    swiftPart.setReadAheadValue(newViewProps.readAheadValue.value);
    newViewProps.readAheadValue.isDirty = false;
  }
  // onValueChange: optional
  if (newViewProps.onValueChange.isDirty) {
    swiftPart.setOnValueChange(newViewProps.onValueChange.value);
    newViewProps.onValueChange.isDirty = false;
  }
  // onValueChangeFinished: optional
  if (newViewProps.onValueChangeFinished.isDirty) {
    swiftPart.setOnValueChangeFinished(newViewProps.onValueChangeFinished.value);
    newViewProps.onValueChangeFinished.isDirty = false;
  }
  // onSegmentChange: optional
  if (newViewProps.onSegmentChange.isDirty) {
    swiftPart.setOnSegmentChange(newViewProps.onSegmentChange.value);
    newViewProps.onSegmentChange.isDirty = false;
  }
  // segments: optional
  if (newViewProps.segments.isDirty) {
    swiftPart.setSegments(newViewProps.segments.value);
    newViewProps.segments.isDirty = false;
  }
  // seekerColors: optional
  if (newViewProps.seekerColors.isDirty) {
    swiftPart.setSeekerColors(newViewProps.seekerColors.value);
    newViewProps.seekerColors.isDirty = false;
  }
  // seekerDimensions: optional
  if (newViewProps.seekerDimensions.isDirty) {
    swiftPart.setSeekerDimensions(newViewProps.seekerDimensions.value);
    newViewProps.seekerDimensions.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
